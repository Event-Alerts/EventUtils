{
  "schemaVersion": 1,
  "id": "eventutils",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "${description}",
  "authors": ["aabss", "srnyx"],
  "contact": {
    "repo": "https://github.com/Event-Alerts/EventUtils"
  },
  "license": "MIT",
  "icon": "assets/icon.png",
  "environment": "*",
  "mixins": [
    "eventutils.mixin.json"
  ],
  "entrypoints": {
    "client": [
      "${group}.eventutils.EventUtils"
    ],
    "modmenu":  [
      "${group}.eventutils.ModMenuImpl"
    ]
  },
  "depends": {
    "minecraft": "${deps_minecraft}",
    "fabricloader": ">=${deps_fabric_loader}",
    "fabric": ">=${deps_fabric_api}",
    "yet_another_config_lib_v3": ">=${deps_yacl}"
  }
}